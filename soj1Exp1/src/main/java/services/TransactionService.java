package services;

import repositories.TransactionRepository;

//partea logica + procesarea (controllers) in pachetul services
public class TransactionService {

    public TransactionRepository transactionRepository;
}
